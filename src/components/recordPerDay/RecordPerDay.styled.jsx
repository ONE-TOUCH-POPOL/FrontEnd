import styled from "styled-components";

const OneRecordPerDay = styled.li`
  text-align: left;
  margin: 4px;
`;
const StyledButton = styled.button`
  cursor: pointer;
  font-size: 15px;
`;

export { OneRecordPerDay, StyledButton };
