/* eslint-disable */
import React from "react";
import BigCalendar from "./BigCalendar";
const BigCalendarComponent = ({ children, click, ...props }) => {
  return <BigCalendar></BigCalendar>;
};

export default BigCalendarComponent;
